***Got it - so X people are power users active querying***

In our model each user would get their dedicated compute instance. THat eliminates the resource contention where queries queue up, and gives you more control over the cost since each user/team has their own sizing?

